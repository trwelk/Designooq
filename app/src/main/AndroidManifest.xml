<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Tregaki.designooq">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DesignerHomeActivity"></activity>
        <activity android:name=".DesignerAccountActivty" />
        <activity android:name=".CustomerAccountActivity" />
        <activity android:name=".CustomerRegistrationActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".MessageListActivity" />
        <activity android:name=".DesignerRegistrationActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>