<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Tregaki.designooq">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MessageListActivity"></activity>
        <activity android:name=".DesignerRegistrationActivity" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".DesignerNewPromotion"/>
        <activity android:name=".DesignerNewDesigns" />
        <activity android:name=".DesignerMyPromotions"/>
        <activity android:name=".DesignerMyDesigns">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>